{
    "num_train_epochs": 1,
    "per_device_train_batch_size": 1024,
    "output_dir": "./saves/ckpt",
    "metrics": ["auc", "logloss"],
    "learning_rate": 0.1,
    "weight_decay": 0.0,
    "save_steps": 500,
    "logging_steps": 50,
    "disable_tqdm": true
}