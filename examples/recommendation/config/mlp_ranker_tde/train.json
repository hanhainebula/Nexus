{
    "num_train_epochs": 1,
    "max_steps": 50,
    "per_device_train_batch_size": 1024,
    "output_dir": "./saves/recommender_results/mlp_ranker",
    "checkpoint_steps": 3000,
    "metrics": ["auc", "logloss"], 
    "optimizer": "adam"
}